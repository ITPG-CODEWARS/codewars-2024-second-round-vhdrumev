<?php

// The best language to exist ever!

// Get URL Path
/*
$currentURL  = $_SERVER['REQUEST_URI'];
$parsedURL   = parse_url($currentURL);
$currentPath = ['path'];

if (is_array($currentPath)) {
    echo "Current URL Path is an array: " . implode(', ', $currentPath);
} else {
    echo "Current URL Path: " . $currentPath;
}
*/

// Just include the file for connecting to the database
include "./connection.php";


